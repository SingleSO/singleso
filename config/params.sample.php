<?php

return [
	// These variables van be set here in config or read from database.
	// 'adminEmail' => 'admin@example.com',
	// 'applicationName' => 'Single Sign On',

	// Set to non-empty string to set domain global cookie to check logged in.
	// 'global.auth.cookie.name' => 'SSO',

	// An array of legacy hashing function.
	// 'legacy.password.hashes' => [
	// 	function($password, $hash, $user) {
	// 		// An example for md5 pasword hashes.
	// 		return md5($password) === $hash;
	// 	},
	// ],
];
